system: "你是脚本结构规划师，只输出 JSON 数组。"
user: |
  请根据以下选题和硬核事实清单，生成一个适合 B 站视频脚本的 4-6 个章节大纲。
  要求：
  - 每个章节标题简洁明了（10字以内）
  - 符合叙事逻辑：开场引入 → 发展过程 → 关键转折 → 当代影响/反思
  - 输出为 JSON 数组，例如：["开场引入", "发家史", "关键转折", "当代影响"]

  选题：{topic}

  事实清单：
  {facts_summary}

  只输出 JSON 数组，不要其他说明。

